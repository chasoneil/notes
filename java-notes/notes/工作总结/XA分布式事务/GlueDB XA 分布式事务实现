
一 2PC的思想  两阶段提交

    参与角色： TM 事务协调者；RM 事务参与者
    执行步骤： 阶段1 -> TM 询问 RM 是不是可以提交? RM 执行 记录 redo undo 返回可以
             阶段2 -> TM 发送提交命令  RM 执行提交 提交完成则返回 ACK 当所有的RM 返回ACK 事务结束

    特点： 2PC 的回滚只有第一阶段有 RM 返回失败 第二阶段 TM 发送 rollback才能回滚
